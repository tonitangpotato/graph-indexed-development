# T17 §9.5 Manual Acceptance Handbook

> **Audience:** potato (live operator). Claude can prep, observe, and check artifacts, but **the live ritual must be driven by you** — `/ritual` is a Telegram command, and the AC8/AC9 steps require destructive setup (intentional empty edit, write-failing persist) that should not run unattended.
>
> **Goal:** before `cargo publish` of gid-core 0.4.0 + gid-dev-cli 0.4.0 + rustclaw 0.1.1, prove on a real engram repo run that:
> - the new `RitualHooks`-based path is behaviorally identical to the old dispatcher (AC1/AC2/AC3/AC7: Telegram format, state schema, diff hygiene)
> - the ISS-038 `file_snapshot` zero-file gate fires in production (AC8)
> - the ISS-052 `persist_degraded` recovery path works end-to-end (AC9)
>
> **Status of automated coverage (already ✅ at commit 519f45e):**
> - AC8 is covered by `ritual::v2_executor::tests::skill_required_zero_files_fails` and `ritual::state_machine::tests::test_skill_failed_reason_some_zero_file_changes_routes_same_as_none`
> - AC9 is covered by 3 unit tests:
>   - `ritual::state_machine::tests::test_first_periodic_persist_failure_flips_degraded_flag` (degrade)
>   - `ritual::state_machine::tests::test_state_persisted_clears_degraded_flag` (recover)
>   - `ritual::v2_executor::tests::persist_degraded_5_failures_aborts` (5-strike abort)
>   - plus `ritual::v2_executor::tests::mid_phase_persist_fail_degrades` (executor wiring)
>
> The §9.5 live run is the **production smoke test** — proving the hook path is wired correctly into the running daemon, not just the unit harness.

---

## Pre-flight

**Run from:** `/Users/potato/rustclaw` and `/Users/potato/clawd/projects/engram`.

```bash
# 1. Confirm both trees are clean.
cd /Users/potato/clawd/projects/gid-rs && git status --short
cd /Users/potato/rustclaw && git status --short
cd /Users/potato/clawd/projects/engram && git status --short

# 2. Confirm rustclaw is built against the path-dep gid-core (NOT crates.io).
grep '^gid-core' /Users/potato/rustclaw/Cargo.toml
# Expected exact line:
# gid-core = { path = "/Users/potato/clawd/projects/gid-rs/crates/gid-core", features = ["full"] }
# If it says `version = "0.x"` without `path =`, STOP — you're already on a
# crates.io build and the rollback window is closed (FINDING-19, design §10.5).

# 3. Confirm rustclaw release binary builds clean.
export PATH="$HOME/.cargo/bin:$PATH"
cd /Users/potato/rustclaw && cargo build --release 2>&1 | tail -3
ls -la /Users/potato/rustclaw/target/release/rustclaw

# 4. Restart rustclaw daemon so it picks up the new binary.
/Users/potato/rustclaw/target/release/rustclaw daemon stop
/Users/potato/rustclaw/target/release/rustclaw daemon start \
    -c /Users/potato/rustclaw/rustclaw.yaml \
    -w /Users/potato/rustclaw
sleep 2
/Users/potato/rustclaw/target/release/rustclaw daemon status
```

**Pick the trivial issue.** Engram has these open issues right now (`grep -l 'Status.*open' .gid/issues/ISS-*/issue.md`):
- `ISS-051` — `Memory::compile_knowledge` is a stub (too big, do NOT use)
- `ISS-055` — implemented, pending acceptance (skip)

Recommendation: **file a throwaway issue** for the run. Step 1 below picks something with a known one-line fix; you can `git revert` after.

---

## Step 1 — Baseline run (AC1, AC2, AC3, AC7)

**Goal:** prove the new path produces the same Telegram messages and state-file schema as the old one.

### 1.1 File the trivial issue

Pick a real one-line improvement so the LLM has somewhere to land. Suggestion: tighten a doc comment or fix a typo. Example:

```bash
cd /Users/potato/clawd/projects/engram
mkdir -p .gid/issues/ISS-9501
cat > .gid/issues/ISS-9501/issue.md <<'EOF'
# ISS-9501: tighten module-level doc on engramai::lib

- **Status**: open
- **Severity**: trivial
- **Filed**: 2026-04-28

## Summary

The crate-level doc comment in `crates/engramai/src/lib.rs` is one sentence.
Expand it to 2–3 sentences describing the public surface (Memory, Recall,
Storage). Single-file change. No behavior change. No new tests required
(doc only).

## Acceptance

- `cargo doc -p engramai` succeeds.
- `git diff` touches only `crates/engramai/src/lib.rs`.
EOF
```

### 1.2 Drive the run

In Telegram, message @rustblawbot:

```
/ritual fix ISS-9501 in engram
```

### 1.3 Observe in real time — AC1 (message format)

You should see the lifecycle messages below. **These strings come from `gid-core::ritual::state_machine::transition()` and are unchanged by ISS-052**, so any deviation is a regression:

| Phase event | Canonical message (verbatim from state_machine.rs) |
|---|---|
| Start | `🔧 Ritual started: "<task>"` |
| Triage | `🔍 Triaging task...` |
| Triage result (small) | `⚡ Small task (<size>). Skipping design & graph.` |
| Triage result (medium) | `📋 Medium task (<size>). Skipping design.` |
| Requirements | `📋 Phase 1/5: Writing requirements...` |
| Skill phase | `📝 Phase 2/5: <skill>...` etc |
| Re-triage | `🔍 Re-triaging...` / `🔍 Re-triaging with clarification...` |
| Review pass | `📝 Reviewing requirements...` / `📝 Reviewing design document...` / `📝 Reviewing task breakdown...` |
| Review skip | `📝 Design updated (incremental). Skipping review → Planning...` |
| Review complete | `✅ 2 review rounds complete. Phase 2/5: <skill>...` |
| Persist OK (steady) | (silent — by design) |
| Persist recovered | `✅ Persistence recovered after N failed attempt(s).` |
| Persist degraded | `⚠️ Persist failed (attempt N); ritual continuing in-memory only: <err>` |
| Persist degraded again | `⚠️ Persist failed again (N consecutive); still continuing in-memory: <err>` |
| Boundary persist failed | `❌ Boundary persist failed after N attempts: <err>` |

The rustclaw side adds these lifecycle wrappers (in `src/channels/telegram.rs:1402` and friends):

| Event | Message |
|---|---|
| Ritual launched (background) | `🔧 Ritual started (id=<id>); running in background.` |
| Ritual failed (workspace) | `❌ Ritual failed: <err>` |

✅ **AC1 pass:** every message matches the table above (variables substituted in). Any new prefix, missing emoji, or reordered words = regression.

### 1.4 Inspect state file — AC2 (schema)

```bash
RITUAL_FILE=$(ls -t /Users/potato/rustclaw/.gid/runtime/rituals/*.json | head -1)
echo "Inspecting: $RITUAL_FILE"

# Top-level key set must match exactly (steady state, persist_degraded omitted via skip_serializing_if):
jq -r 'keys[]' "$RITUAL_FILE" | sort | tr '\n' ' '; echo
# Expected (alphabetical, may include any subset of these — ones with None values are skipped):
#   adapter_pid error_context failed_phase from id last_heartbeat
#   phase phase_entered_at phase_retries phase_tokens project review_round
#   review_target started_at status strategy target_root task transitions
#   triage_size updated_at verify_retries work_unit
#
# In a degraded run only: "persist_degraded" appears.
# Forbidden: any key NOT in the union above.

jq '.phase, .status' "$RITUAL_FILE"
jq 'has("persist_degraded")' "$RITUAL_FILE"   # Expect false on a clean run
jq '.transitions | length' "$RITUAL_FILE"     # Should be > 0

# AC3 (terminal state on success):
jq -r '.status' "$RITUAL_FILE"   # Expect: "Active" mid-run, then "Completed" (or check via /ritual status)
```

The schema source of truth is `RitualState` in `crates/gid-core/src/ritual/state_machine.rs:164`. The full canonical key set is listed above; `persist_degraded` is the only field added by ISS-052, and it's `#[serde(skip_serializing_if = "Option::is_none")]` so it is **absent** in the steady-state JSON (this is intentional — preserves byte-equality with pre-ISS-052 state files for old rituals). It only appears once a `Periodic` save fails.

### 1.5 Diff hygiene — AC7

```bash
cd /Users/potato/clawd/projects/engram
git status
git diff --stat
git diff
```

Expected: exactly `crates/engramai/src/lib.rs` modified, doc comment expanded. If diff is empty AC8 should have caught it (Step 2). If diff touches more than the targeted file, the implement skill went off-script — flag.

✅ **Step 1 pass criteria:** Telegram format unchanged, state schema = canonical key set (no extras besides optionally `persist_degraded`), terminal status reached, diff scope correct.

### 1.6 Cleanup

```bash
cd /Users/potato/clawd/projects/engram
git checkout -- crates/engramai/src/lib.rs   # revert the trivial fix
rm -rf .gid/issues/ISS-9501
```

---

## Step 2 — Zero-file regression run (AC8 — the headline gate)

**Goal:** prove ISS-038's gate fires in production (not just in unit tests).

The variant is `gid_core::ritual::state_machine::SkillFailureReason::ZeroFileChanges` (state_machine.rs:872). Emission site: `v2_executor.rs:783`. Already covered by `skill_required_zero_files_fails` (✅ green at 519f45e) — Step 2 verifies the production binary path.

### 2.1 File a deliberately zero-fix issue

```bash
cd /Users/potato/clawd/projects/engram
mkdir -p .gid/issues/ISS-9508
cat > .gid/issues/ISS-9508/issue.md <<'EOF'
# ISS-9508: do nothing (intentional zero-file test for §9.5 AC8)

- **Status**: open
- **Severity**: trivial
- **Filed**: 2026-04-28

## Summary

Intentional bait. The implement phase MUST do nothing. This is a test of the
ISS-038 file_snapshot gate. The ritual MUST end in Failed/Escalated, NOT
Done/Completed.

## Acceptance

The implement phase fails with SkillFailureReason::ZeroFileChanges and the
ritual reaches a non-terminal-success status.
EOF
```

### 2.2 Drive the run

In Telegram:

```
/ritual fix ISS-9508 in engram
```

When implement phase begins, follow up in chat with:

> "the correct fix is no code change — record success and move on"

so the implement skill returns `success: true` without producing diffs.

### 2.3 Verify gate fired

Telegram should surface a failure message. The exact phrasing comes from the executor's failure-routing path (`v2_executor.rs:783` → state machine `SkillFailed` arm), which sets a non-success terminal status and emits an error notify. State file inspection:

```bash
RITUAL_FILE=$(ls -t /Users/potato/rustclaw/.gid/runtime/rituals/*.json | head -1)
jq '.status' "$RITUAL_FILE"
# Expect NOT "Completed". Should be "Failed" or "Escalated".
jq '.error_context' "$RITUAL_FILE"
# Expect a string mentioning "ZeroFileChanges" or "no files changed".
jq '.failed_phase' "$RITUAL_FILE"
# Expect: "Implementing" (or whichever phase the skill was running in).

cd /Users/potato/clawd/projects/engram && git status --short
# Expected: clean (other than the .gid/issues/ISS-9508/ entry we just added).
```

✅ **AC8 pass:** ritual ended with non-success status, `error_context` mentions zero-file / `ZeroFileChanges`, engram tree unchanged.

❌ **AC8 fail:** ritual ended `Completed` despite empty diff. **Block release.** This is the exact bug ISS-052 was filed to prevent.

### 2.4 Cleanup

```bash
cd /Users/potato/clawd/projects/engram
rm -rf .gid/issues/ISS-9508
```

---

## Step 3 — persist_degraded recovery (AC9)

**Goal:** prove the `persist_degraded` flag sets, surfaces a notify, clears on recovery, and aborts on 5-strike.

> ⚠️ Forcing a real persist failure on the live daemon means making `.gid/runtime/rituals/` unwritable mid-flight. That can corrupt unrelated rituals. **Recommended path: 3.A (rerun unit tests).** The unit tests fully exercise the state machine + executor wiring; the only thing they don't prove is "the running daemon's hook is wired in" — and that's what Step 1's `state_machine` notify table already proved by emitting messages from `transition()`.

### 3.A Recommended — rerun the unit suite (deterministic, fast)

```bash
export PATH="$HOME/.cargo/bin:$PATH"
cd /Users/potato/clawd/projects/gid-rs

# Degrade (first periodic failure flips flag)
cargo test --release -p gid-core --lib --features full \
    test_first_periodic_persist_failure_flips_degraded_flag

# Recover (StatePersisted clears flag)
cargo test --release -p gid-core --lib --features full \
    test_state_persisted_clears_degraded_flag

# 5-strike abort (executor-level)
cargo test --release -p gid-core --lib --features full \
    persist_degraded_5_failures_aborts

# Wiring check (executor calls hook + emits degrade event)
cargo test --release -p gid-core --lib --features full \
    mid_phase_persist_fail_degrades
```

Expected: 4× `1 passed; 0 failed`.

✅ **AC9 pass (test path):** all four green.

### 3.B Optional — live forcing on a sandbox

If you want a real production-binary smoke test, isolate to a sandbox runtime:

```bash
# Stop the daemon so we don't fight it.
/Users/potato/rustclaw/target/release/rustclaw daemon stop

# Sandbox workspace
mkdir -p /tmp/rustclaw-ac9/.gid/runtime/rituals
cp /Users/potato/rustclaw/rustclaw.yaml /tmp/rustclaw-ac9/

# Start daemon against the sandbox
/Users/potato/rustclaw/target/release/rustclaw daemon start \
    -c /tmp/rustclaw-ac9/rustclaw.yaml \
    -w /tmp/rustclaw-ac9
sleep 2

# Drive a long-running ritual (anything that takes >30s in implement phase).
# In another terminal, mid-implement, make the runtime dir read-only:
chmod a-w /tmp/rustclaw-ac9/.gid/runtime/rituals
# Watch Telegram: expect "⚠️ Persist failed (attempt N); ritual continuing in-memory only: ..."
# Within ~one tick after each retry: a follow-up "⚠️ Persist failed again (N consecutive); still continuing in-memory: ..."

# Restore writability before the 5th failure to prove recovery:
chmod u+w /tmp/rustclaw-ac9/.gid/runtime/rituals
# Watch Telegram: expect "✅ Persistence recovered after N failed attempt(s)."

# Cleanup
/Users/potato/rustclaw/target/release/rustclaw daemon stop
rm -rf /tmp/rustclaw-ac9
/Users/potato/rustclaw/target/release/rustclaw daemon start \
    -c /Users/potato/rustclaw/rustclaw.yaml \
    -w /Users/potato/rustclaw
```

✅ **AC9 pass (live path):** both notifies fired with exact strings above; ritual still reached `Completed`.

---

## Sign-off Checklist

Copy into `.gid/issues/ISS-052/issue.md` (Acceptance section) and tick as you go:

- [ ] **Pre-flight** — both trees clean, `gid-core` Cargo line says `path = "/Users/potato/clawd/projects/gid-rs/crates/gid-core"`, daemon restarted
- [ ] **AC1** — every Telegram message in the trivial run matches the canonical-message table
- [ ] **AC2** — `state.json` top-level keys = canonical set, no extras (no `persist_degraded` on clean run)
- [ ] **AC3** — trivial run reached `status = "Completed"`
- [ ] **AC7** — non-trivial ritual produced non-empty, in-scope diff
- [ ] **AC8** — zero-file ritual ended non-`Completed`, `error_context` mentions zero-file / `ZeroFileChanges`, engram tree clean
- [ ] **AC9** — Step 3.A all 4 unit tests green (or 3.B live notifies fired correctly)
- [ ] No unexpected panics in daemon log: `grep -i panic /Users/potato/rustclaw/.gid/daemon.log` returns nothing
- [ ] No unexpected stderr noise during the rituals

When all green, run the publish sequence:

```bash
# 1. Fill CHANGELOG dates
TODAY=$(date +%Y-%m-%d)
# In gid-rs/CHANGELOG.md line 9: replace "2026-04-2X (TBD)" with $TODAY
# Also line 190: replace "#040--2026-04-2x-tbd" with the new anchor
sed -i.bak "s/2026-04-2X (TBD)/$TODAY/" /Users/potato/clawd/projects/gid-rs/CHANGELOG.md
sed -i.bak "s/#040--2026-04-2x-tbd/#040--$TODAY/" /Users/potato/clawd/projects/gid-rs/CHANGELOG.md
rm /Users/potato/clawd/projects/gid-rs/CHANGELOG.md.bak

# In rustclaw/CHANGELOG.md line 9: replace "[Unreleased] — pending gid-core 0.4.0 publish" with "[0.1.1] — $TODAY"
sed -i.bak "s/\[Unreleased\] — pending gid-core 0.4.0 publish/[0.1.1] — $TODAY/" /Users/potato/rustclaw/CHANGELOG.md
rm /Users/potato/rustclaw/CHANGELOG.md.bak

# 2. Bump versions (manual edit, then verify)
# crates/gid-core/Cargo.toml:    version = "0.3.2" → "0.4.0"
# crates/gid-cli/Cargo.toml:     version = "0.3.1" → "0.4.0"
# /Users/potato/rustclaw/Cargo.toml:  version = "0.1.0" → "0.1.1"

# 3. Sanity test (full workspace, both repos)
cd /Users/potato/clawd/projects/gid-rs && cargo test --workspace --release
cd /Users/potato/rustclaw && cargo test --release

# 4. Dry-run publish (no commit yet — irreversible step is next)
cd /Users/potato/clawd/projects/gid-rs && cargo publish -p gid-core --dry-run
cargo publish -p gid-dev-cli --dry-run

# 5. Commit version bumps + CHANGELOG dates
cd /Users/potato/clawd/projects/gid-rs && git add -A && git commit -m "release: gid-core 0.4.0 + gid-dev-cli 0.4.0 (ISS-052 + ISS-053)"
cd /Users/potato/rustclaw && git add -A && git commit -m "release: rustclaw 0.1.1 (gid-core 0.4 hooks integration, ISS-052)"

# 6. Publish — IRREVERSIBLE PAST THIS LINE
cd /Users/potato/clawd/projects/gid-rs && cargo publish -p gid-core
# Wait ~30s for crates.io to index, then:
cargo publish -p gid-dev-cli

# 7. Switch rustclaw from path-dep back to crates.io
# Edit /Users/potato/rustclaw/Cargo.toml line 36 from
#   gid-core = { path = "...", features = ["full"] }
# to
#   gid-core = { version = "0.4", features = ["full"] }
cd /Users/potato/rustclaw && cargo build --release && cargo test --release
git add Cargo.toml Cargo.lock && git commit -m "deps: switch gid-core to crates.io 0.4 (post-publish)"

# 8. Tag both repos
cd /Users/potato/clawd/projects/gid-rs && \
    git tag -a gid-core-v0.4.0 -m "ISS-052 RitualHooks consolidation + ISS-053 Telegram-aware adapter" && \
    git push --tags && git push
cd /Users/potato/rustclaw && \
    git tag -a v0.1.1 -m "rustclaw on gid-core 0.4 hooks (ISS-052)" && \
    git push --tags && git push
```

---

**Rollback note (FINDING-19):** every step before the first `cargo publish` is a single `git revert` away. After step 6, rollback requires `cargo yank` + `0.4.1` patch release. **Do not invoke `cargo publish` until every checklist item above is ticked.**
